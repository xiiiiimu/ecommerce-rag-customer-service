<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.knowledge_system.mapper.CustomerOrderMapper">

    <resultMap id="CustomerOrderMap" type="com.example.knowledge_system.entity.CustomerOrder">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="orderNo" column="order_no"/>
        <result property="productName" column="product_name"/>
        <result property="orderStatus" column="order_status"/>
        <result property="payTime" column="pay_time"/>
        <result property="shipTime" column="ship_time"/>
        <result property="logisticsStatus" column="logistics_status"/>
        <result property="refundStatus" column="refund_status"/>
    </resultMap>

    <select id="findByUserId" resultMap="CustomerOrderMap">
        SELECT id, user_id, order_no, product_name, order_status, pay_time, ship_time, logistics_status, refund_status
        FROM customer_order
        WHERE user_id = #{userId}
        ORDER BY id DESC
    </select>

    <select id="findByOrderNo" resultMap="CustomerOrderMap">
        SELECT id, user_id, order_no, product_name, order_status, pay_time, ship_time, logistics_status, refund_status
        FROM customer_order
        WHERE order_no = #{orderNo}
        LIMIT 1
    </select>

</mapper>